StarForge-AI 是一套模块化、单一职责的端到端 AI 应用平台。
它复用业界领先的开源成果，包括 FastAPI、FAISS、sentence-transformers、Ollama 与 OpenAI 兼容协议。
核心能力涵盖：文档摄入与切分、文本向量化、向量检索、RAG 生成管线，以及可选的工具编排 Agent。
系统默认提供确定性 Mock 后端，可在无网络、无密钥的干净环境中一键复现并跑通完整链路。
真实 LLM 与向量模型可通过环境变量无缝切换接入，无需改动代码。
每个功能模块仅暴露一个抽象接口（ABC），模块间通过依赖注入容器解耦，可独立替换与独立验证。
作者：晨星。
